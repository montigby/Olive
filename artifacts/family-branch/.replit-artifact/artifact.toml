kind = "web"
previewPath = "/"
title = "FamilyBranch"
version = "1.0.0"
id = "artifacts/family-branch"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25826

[services.development]
run = "pnpm --filter @workspace/family-branch run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/family-branch", "run", "build" ]
publicDir = "artifacts/family-branch/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25826"
BASE_PATH = "/"
